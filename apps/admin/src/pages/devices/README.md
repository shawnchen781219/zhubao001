# Devices Page

Offline page boundary for device status monitoring.

Responsibilities:

- Represent future `GET /admin/devices` response.
- Surface active, pending, suspended, and retired devices.
- Make heartbeat age visible for operational follow-up.

Current status: no React page implementation, no UI, no network request.
