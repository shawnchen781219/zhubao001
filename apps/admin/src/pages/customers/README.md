# Customers Page

Offline page boundary for the customer list.

Responsibilities:

- Represent future `GET /admin/customers` response.
- Enforce store-scoped staff context.
- Surface sparse customer profiles from scan authorization.

Current status: no React page implementation, no UI, no network request.
