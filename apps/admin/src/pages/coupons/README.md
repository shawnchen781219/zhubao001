# Coupons Page

Offline page boundary for issued coupons.

Responsibilities:

- Represent future `GET /admin/coupons` response.
- Distinguish issued, redeemed, expired, and voided states.
- Preserve auditability of claim/redeem source.

Current status: no React page implementation, no UI, no network request.
