# Try-On Sessions Page

Offline page boundary for try-on session records.

Responsibilities:

- Represent future `GET /admin/try-on-sessions` response.
- Show anonymous, scanned, authorized, completed, and expired statuses.
- Link session records to customer and device summaries when available.

Current status: no React page implementation, no UI, no network request.
