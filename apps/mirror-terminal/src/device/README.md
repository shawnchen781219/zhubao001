# Device Boundary

Offline trusted device boundary.

Responsibilities:

- Hold future device registration and heartbeat contracts.
- Provide `X-Device-Id`, `X-Device-Signature`, and `X-Request-Id` inputs after implementation.

Current status: no credential storage, no signature generation, no network request.
